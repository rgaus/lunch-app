<h1>Where should we eat?</h1>
<p>Enter the criteria to filter by:</p>

<a href="https://docs.google.com/spreadsheets/d/<%= sheetId %>">Or add to the list of places</a>

<!-- All form fields -->
<form method="GET">
  <input type="hidden" name="pick" value="true" />

  <% for (var field in fields) { %>
    <% if (field !== "id" && field !== "venue") { %>
      <p class="form-group">
        <label for="<%= field %>"><%= field %></label>
        <select name="<%= field %>">
        <% fields[field].forEach(function(f) { %>
          <option value="<%= f %>"><%= f %></option>
        <% }) %>
        </select>
      </p>
    <% } %>
  <% } %>

  <input type="submit" />
</form>
